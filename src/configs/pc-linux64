CONFIG = pc-linux64
BITS = 64

CPP_FLAGS += -O3
#CPP_FLAGS += -ffast-math
CPP_FLAGS += -fomit-frame-pointer
CPP_FLAGS += -fopenmp
CPP_FLAGS += -DNDEBUG

LINK_FLAGS += -s

GL_LIBS = -lGL -lglut -lGLEW
FCOLLADA_LIB = ../../lib/pc-linux64/libfcollada.a -lxml2

