CONFIG = pc-linux64-debug
BITS = 64

CPP_FLAGS += -O0
CPP_FLAGS += -g
CPP_FLAGS += -D_DEBUG

CORE_LIBS = \
	-lstdc++ \
	../../bin/$(CONFIG)/libLightsprintCore.so \
	-lrt \
	-lboost_filesystem \

GL_LIBS = \
	../../bin/$(CONFIG)/libLightsprintGL.so \
	-lGL \
	-lGLU \
	-lGLEW

IO_LIBS = \
	../../bin/$(CONFIG)/libLightsprintIO.so \
	-lfreeimage \
	-lboost_serialization \
	-lboost_iostreams
#	-lxml2 \
#	/../../lib/pc-linux64/libfcollada.a \
