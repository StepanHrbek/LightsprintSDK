varying vec4 projCoord;

void main()
{
  vec4 realPos = gl_ModelViewMatrix * gl_Vertex;
  projCoord = gl_TextureMatrix[0] * realPos;
  gl_Position = ftransform();

#ifdef FORCE_2D_POSITION
  gl_Position = gl_MultiTexCoord7.xy;
#endif
}
