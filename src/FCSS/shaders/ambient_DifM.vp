varying vec2 diffuseCoord;

void main()
{
  diffuseCoord = gl_MultiTexCoord0.xy;
  gl_FrontColor = gl_Color;
  gl_FrontColor *= 2.0; // bost indirect brightness. this is not realistic
  gl_Position = ftransform();
}
